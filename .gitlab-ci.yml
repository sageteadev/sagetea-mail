stages:
  - build
  - deploy

variables:
  DOCKER_DRIVER: overlay2
  CLICKABLE_VERSION: 6.23.0

.arm64: &arm64
  variables:
    ARCH: "arm64"
    ARCH_TRIPLET: "aarch64-linux-gnu"

.sageteamail:
  stage: build
  image: clickable/amd64-16.04-$ARCH:16.04.4-qt5.9
  tags:
    - docker
  script:
    - clickable build
  artifacts:
    paths:
      - 'build/$ARCH_TRIPLET/*.click'
    when: on_success
    expire_in: 4 days

sageteamail:arm64:
  <<: *arm64
  extends: .sageteamail